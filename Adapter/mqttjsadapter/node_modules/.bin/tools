#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@google-cloud/nodejs-repo-tools/bin/tools" "$@"
  ret=$?
else 
  node  "$basedir/../@google-cloud/nodejs-repo-tools/bin/tools" "$@"
  ret=$?
fi
exit $ret
